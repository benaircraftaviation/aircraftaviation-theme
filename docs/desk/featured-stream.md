# Featured stream

JSON key: `featured_stream`.

Fields: `name`, `url`, `status`, `description`.

Read `STREAM_BIBLE.md` first. Verify before embed.

- Use a Verified 24/7 row only after Proof writes CONFIRMED.
- If that shelf is empty, do not promote a scheduled show into this slot. Point `url` at the channel you mean, and say in `status` that the player is not verified.
- No search URL. No invented clock.
