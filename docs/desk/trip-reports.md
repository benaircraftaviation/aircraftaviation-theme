# Trip reports

JSON key: `trip_reports`.

Each item: `title`, `url`, `route`, `summary`. Six is the ceiling, not a quota. Empty beats a report you have not read.

Use reports already published by outlets on `SOURCE_LIST.md`, or a B. Harris post on this site. Root-relative URLs for posts on this site (`/blog/your-slug/`).
