# Sidebar news

JSON key: `sidebar_news`.

Short items: `title`, `url`, `date`, `summary`. One or two sentences.

Same grade rule as aviation news. CONFIRMED or LIKELY only. Industry notes (drones, electric aircraft, bizav shows) belong here when they are sourced. WATCH stays in the packet.
