# Newsletter

JSON key: `newsletter`.

`title`, `text` (one line), `button`, `url`.

Point `url` at the staging post first. Use a root-relative path for a post on this site, such as `/blog/your-post-slug/`.

Move the same URL to production only after the letter is the one Voice closed with: See you at the fence. — B. Harris
