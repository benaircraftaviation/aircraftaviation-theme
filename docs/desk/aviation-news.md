# Aviation news

JSON key: `aviation_news`.

Replace the center-column items from the frozen packet. Each item: `title`, `url`, `source`, `date`, `summary`.

Public copy may use CONFIRMED and LIKELY only. WATCH does not go in this list. KILL is omitted.

Prefer the primary page. Name the outlet in `source`.
