# X feed

JSON key: `x_feed`.

Fields: `title`, `handle`, `url`, `placeholder`.

Until `url` is the real Aircraft Aviation account, leave `handle` and `url` empty and keep the placeholder sentence. Do not invent a feed. Do not embed a widget.
