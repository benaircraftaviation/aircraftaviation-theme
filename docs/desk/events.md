# Events

JSON key: `upcoming_events`.

Each item: `title`, `url`, `date`, `location`.

AvGeek events only: spotting days, Cranky events, AOPA, a show that puts new aircraft on the floor, bizjet events. Drop the row when the date has passed.

If the only source is a rumor, leave it off the homepage.
